#pragma once

namespace GlEngine
{
	class GraphicsContext
	{
		GraphicsContext();
		~GraphicsContext();

		// AddGraphicsObject(GraphicsObject)
		// RemoveGraphicsObject(GraphicsObject)

		Update();
	};
}